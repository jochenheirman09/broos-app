// This file is intentionally left blank to resolve a build conflict.
// The (admin) route group is now the source of truth for these pages.
export default function BlankLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
